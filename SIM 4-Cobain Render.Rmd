---
title: "Latihan Latex"
author: "yuliskanurhanif"
date: "2026-04-08"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars, include = FALSE}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.


```{r}

```

#Contoh sederhana
Tekanan(P) berbanding terbalik dengan volume (V), yang dapat ditulis sebagai 
$$P \propto \frac{1}{V}$$.

Persamaan kuadrat memiliki bentuk umum:
$$ax^2 + bx + c = 0$$
Atau menggunakan environment equation:

\begin{equation}
E = mc^2
\end{equation}

Atau menggunakan
```
\[...\]:
```

\[
\int_{-\infty}^{\infty}e^{-x^2} dx = \sqrt{\pi}
\]

contoh menuliskan x-bar : $$\bar{x}$$
contoh menuliskan 1/(x-bar) :$$\frac{1}{\bar{x}}$$

$$\frac{1}{n}\sum_{i=1}^n x_i$$

htmlsederhana dalam html
<p style = "color : red; font-size = 20px;">
Ini adalah teks berwarna merah dengan ukuran 20px!
</p>

<p style = "color : red; font-size = 80px;">
Ini adalah teks berwarna merah dengan ukuran 80px!
</p>

kalau atur font dansebagainya lebih bagus pakai css, kalau html buat heading

kalau menambahkan gambar filenya rmarkdown dan gambar harus 1 lokasi folder, kalau engga alamatnya harus lengkap






